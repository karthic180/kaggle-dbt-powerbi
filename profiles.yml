analytics_profile:
  target: duckdb
  outputs:

    duckdb:
      type: duckdb
      path: project.duckdb
      threads: 1

    sqlite:
      type: sqlite
      threads: 1
      database: project
      schema: main
      schemas_and_paths:
        main: project.db
      schema_directory: .